# npm 12 classifies some registry-served artifacts (e.g. @tailwindcss/oxide wasm
# fallbacks) as "remote" fetches and blocks them by default (EALLOWREMOTE), which
# breaks cold installs. npm 12 has no per-package allowlist (mode is none|all).
allow-remote=all
